# 🛒 GroupBuy Mini App

Telegram Mini App для групповых покупок по модели PinDuoDuo.

## 📋 Описание

Приложение позволяет пользователям объединяться для покупки товаров по оптовым ценам. Чем больше людей присоединяется к сбору — тем ниже цена для всех.

## 🚀 Быстрый старт

### Требования

- Python 3.11+
- Аккаунт Supabase (бесплатный)
- Telegram Bot (от @BotFather)

### 1. Клонирование и установка

```bash
# Клонируй репозиторий
git clone <url>
cd groupbuy-app

# Создай виртуальное окружение
python -m venv venv

# Активируй его
# Windows:
venv\Scripts\activate
# macOS/Linux:
source venv/bin/activate

# Установи зависимости
pip install -r requirements.txt
```

### 2. Настройка Supabase

1. Создай проект на [supabase.com](https://supabase.com)
2. Перейди в SQL Editor
3. Скопируй содержимое `database/init.sql` и выполни
4. Скопируй ключи из Settings → API

### 3. Настройка переменных окружения

```bash
# Скопируй пример
cp .env.example .env

# Открой .env и заполни:
# - TELEGRAM_BOT_TOKEN (от @BotFather)
# - SUPABASE_URL
# - SUPABASE_ANON_KEY
# - SUPABASE_SERVICE_KEY
# - JWT_SECRET (сгенерируй: python -c "import secrets; print(secrets.token_hex(32))")
```

### 4. Запуск

```bash
cd backend

# Режим разработки (с автоперезагрузкой)
uvicorn main:app --reload --port 8000

# Или через Python
python main.py
```

### 5. Проверка

Открой в браузере:
- API: http://localhost:8000
- Документация: http://localhost:8000/docs
- Healthcheck: http://localhost:8000/health

## 📁 Структура проекта

```
groupbuy-app/
├── backend/                 # Серверная часть (Python/FastAPI)
│   ├── main.py             # Точка входа
│   ├── config.py           # Конфигурация
│   ├── database/           # Работа с БД
│   │   ├── connection.py   # Подключение к Supabase
│   │   └── models.py       # Pydantic-модели
│   ├── routers/            # API эндпоинты (TODO)
│   ├── services/           # Бизнес-логика (TODO)
│   └── utils/              # Вспомогательные функции (TODO)
│
├── frontend/               # Клиентская часть (TODO)
│   ├── index.html
│   ├── css/
│   └── js/
│
├── database/
│   └── init.sql            # SQL для создания таблиц
│
├── .env.example            # Пример переменных окружения
├── .gitignore
├── requirements.txt        # Python-зависимости
└── README.md
```

## 🔧 Команды

```bash
# Запуск в разработке
uvicorn main:app --reload

# Запуск тестов
pytest

# Проверка типов
mypy backend/

# Форматирование кода
black backend/
```

## 📚 API Документация

После запуска доступна по адресам:
- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc
- OpenAPI JSON: http://localhost:8000/openapi.json

## 🛠 Технологии

- **Backend**: FastAPI, Python 3.11+
- **Database**: Supabase (PostgreSQL)
- **Payments**: ЮKassa
- **Delivery**: СДЭК API
- **Frontend**: HTML, Tailwind CSS, Vanilla JS

## 📝 TODO

- [ ] Фаза 2: Авторизация и товары
- [ ] Фаза 3: Групповые сборы
- [ ] Фаза 4: Оплата
- [ ] Фаза 5: Фронтенд
- [ ] Фаза 6: Доставка и возвраты
- [ ] Фаза 7: Уведомления
- [ ] Фаза 8: Деплой

## 📄 Лицензия

MIT
